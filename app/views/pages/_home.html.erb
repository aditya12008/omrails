<% unless user_signed_in? %>
	<div class="hero-unit">
		<h1>Welcome to PicShare!</h1><br>
		<p>
			<%= link_to "Sing Up now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
		</p>
	</div>
<% end %>